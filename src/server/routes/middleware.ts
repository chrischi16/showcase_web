/** Enthält Funktionen, die in die Behandlung von eingehenden Requests eingereiht werden können ## */

import express from 'express'
import cookieParser from 'cookie-parser'

// todo: middleware ???